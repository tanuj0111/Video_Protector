{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../frontend && npm install && npm run build && npm install"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}